# Configuração de deploy do Netlify — site estático (um único index.html)
[build]
  publish = "."
  command = ""

# Funções serverless (recebem o lead e repassam ao CRM)
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
